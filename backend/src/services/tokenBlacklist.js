/**
 * قائمة سوداء للتوكنات المُلغاة (in-memory)
 * تُمسح عند إعادة تشغيل الخادم — كافية لضمان صحة تسجيل الخروج خلال جلسة التشغيل.
 */
export const revokedTokens = new Set()

// تنظيف دوري كل ساعة لمنع تراكم الذاكرة
setInterval(() => revokedTokens.clear(), 60 * 60 * 1000)
